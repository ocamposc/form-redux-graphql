const GET_PERSONAS = `
        {
            personas{
                id
                name
                lastName
                age
                email
            }
        }`

module.exports = {
    GET_PERSONAS,
}